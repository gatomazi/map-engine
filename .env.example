ASSETS_DIR=/app/assets
OUTPUT_DIR=/tmp/map-output
IBGE_CACHE_TTL=3600
MAX_LOCALIDADES=10
PREVIEW_MAX_DIM=800
PREVIEW_PINTAR_DPI=120
PREVIEW_SVG_DPI=96
FINAL_DPI=300
# Opcional: DPI fixo para pintar_municipio (CLI). Na API, preview usa PREVIEW_PINTAR_DPI.
# PINTAR_PNG_DPI=300
API_KEY=
LOG_LEVEL=INFO
# Limite de municípios por POST /batch/estado/{uf}
BATCH_MAX_MUNICIPIOS=40
